#include <stdio.h>
#include<string.h>
#include<ctype.h>
#include<malloc.h>
#pragma warning(disable:4996)

char alph[] = "c= c- d- lj nj s= z= dz=";//특수크로아티아 알파벳
char* croatia[9];

int main()
{
   char* token;
   token = strtok(alph, " ");

   int i = 0;
   croatia[i] = token;
   i++;

   while (token != NULL)
   {
      token = strtok(NULL, " ");
      croatia[i] = token;
      i++;
   }//특수 크로아티아 알파벳 배열

   char input[101];
   scanf("%s", input);

   int a = strlen(input);
   int count1 = 0;
   int count2 = 0;
   
   int j = 0;

   while(input[j+1]!='\0')//두글자 특수 크로아티아 알파벳인 경우
   {
      char s[3] = {};
      s[0] =  input[j];
      s[1] = input[j + 1];
      s[2] = '\0';

      for (int z = 0; z < 7; z++)
      {
         if (strcmp(s, croatia[z]) == 0)
         {

            count1 ++;
            break;
         }
      }
      j++;
   }

   j = 0;

   while (input[j + 2] != '\0')//3글자인 특수 크로아티아 알파벳인 경우
   {
      char s[4] = {};
      s[0] = input[j];
      s[1] = input[j + 1];
      s[2] = input[j + 2];
      s[3] = '\0';



   if (strcmp(s, croatia[7]) == 0)//dz=랑 z=겹치니까 z=인경우 빼기
      {
         count2 ++;
         count1--;
      }
   j++;
   }

   a = a - ((count1 * 2) + (count2 * 3));//a는 특수 크로아티아 알파벳이 아닌 한글자짜리 알파벳
   printf("%d", a+count1+count2); // 세가지 경우를 다더한다


   return 0;
}